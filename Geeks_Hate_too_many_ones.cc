#include <bits/stdc++.h>
using namespace std;

void max_query(int n) {
    char str[33];
    itoa(n, str, 2);
    cout<<str;

    

}

int main() {
    
    int n;
    cin>>n;
    max_query(n);
    return 0;
}